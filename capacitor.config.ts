import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AntoniusKevin_00000045444_IF670_A_UTS',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
